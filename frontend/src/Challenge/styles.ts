import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  sidebarCard: {
    'margin-bottom': '20px',
    padding: '30px'
  },

  mainContainer: {
    padding: '30px'
  },

  title: {
    marginBottom: '20px',
    textAlign: 'center'
  },
});

export default useStyles;