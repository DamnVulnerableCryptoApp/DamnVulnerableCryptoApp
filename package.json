{
  "name": "damn_vulnerable_crypto_app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "yarn": "^1.22.4"
  },
  "scripts": {
    "build-react": "cd frontend && yarn run test:lint && yarn run build",
    "build-be": "cd backend && yarn run test:lint && yarn build",
    "build": "yarn install-deps && yarn run build-react && yarn run build-be && yarn run merge-projects",
    "copy-fe-to-public": "cp -r frontend/build/ backend/dist/public",
    "merge-projects": "yarn run copy-fe-to-public",
    "install-deps:be": "cd backend && yarn install",
    "install-deps:react": "cd frontend && yarn install",
    "install-deps": "yarn run install-deps:be && yarn run install-deps:react",
    "start": "cd backend && yarn run start:prod",
    "clean": "rm -rf dist && rm -rf backend/dist && rm -rf frontend/build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/luisfontes19/damn_vulnerable_crypto_app.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/luisfontes19/damn_vulnerable_crypto_app/issues"
  },
  "homepage": "https://github.com/luisfontes19/damn_vulnerable_crypto_app#readme"
}